# Coop-CBM 实验步骤与流程

> 论文：Auxiliary Losses for Learning Generalizable Concept-based Models (NeurIPS 2023)
> 数据集路径：`src/data/CUB_200_2011`

---

## Step 0：环境依赖安装

```bash
pip install torch torchvision
pip install scikit-learn scipy numpy matplotlib
pip install imagenet-c          # 图像损坏实验需要
pip install Pillow pandas
```

最低版本要求：
- Python >= 3.8
- PyTorch >= 1.10
- torchvision >= 0.11
- scikit-learn >= 0.24

---

## Step 1：数据集准备

### 1.1 当前数据目录结构

```
src/data/CUB_200_2011/
├── images/              ← 200类鸟类图像（已存在）
├── images.txt           ← 图像ID→路径映射（已存在）
├── train_test_split.txt ← 训练/测试划分（已存在）
└── classes.txt          ← 类别名称（已存在）
```

### 1.2 补充缺失的 attributes 目录（必须）

从 CUB-200-2011 官方数据集完整压缩包中获取 `attributes` 目录，复制到项目：

```bash
cp -r /path/to/CUB_200_2011/attributes  src/data/CUB_200_2011/attributes/
```

补充后的完整目录结构：

```
src/data/CUB_200_2011/
├── images/
├── images.txt
├── train_test_split.txt
├── classes.txt
└── attributes/
    ├── attributes.txt                          ← TTI评估需要（312行属性名）
    └── class_attribute_labels_continuous.txt   ← 训练需要（200×312连续值矩阵）
```

---

## Step 2：代码 Bug 修复

### 2.1 `experiments.py` —— 允许列表缺少 'Coop'，且 `.upper()` 破坏大小写

**原代码（有问题）：**
```python
assert sys.argv[2] in ['Concept_XtoC', ..., 'HyperparameterSearch']
experiment = sys.argv[2].upper()   # 'Coop' 变成 'COOP'，后续匹配失败
```

**修复后：**
```python
assert sys.argv[2] in ['Concept_XtoC', 'Independent_CtoY', 'Sequential_CtoY',
                       'Standard', 'StandardWithAuxC', 'Multitask', 'Joint', 'Coop',
                       'Probe', 'TTI', 'Robustness', 'HyperparameterSearch']
experiment = sys.argv[2]   # 保留原始大小写
```

同时在 `run_experiments` 函数中添加 `'Coop'` 的路由分支：
```python
elif experiment == 'Multitask' or experiment == 'Coop':
    train_X_to_Cy(*args)
```

### 2.2 `src/train.py` —— 缺少模块导入

`train_probe`、`test_time_intervention` 等函数调用了未导入的模块，在文件头部添加：

```python
from src.model import probe, hyperopt
from src.eval import tti
from src.data import gen_spurious
```

### 2.3 设置工作目录与 PYTHONPATH

所有命令必须在**项目根目录**下运行：

```bash
cd /media/pang/U/master_project/coop-cbm-main
export PYTHONPATH=$(pwd):$PYTHONPATH
```

---

## Step 3：实验一 —— Standard 黑盒基线

训练不使用任何概念的标准 InceptionV3 分类器，作为性能上界参考。

```bash
python3 experiments.py CUB Standard --seed 1 \
    -log_dir outputfiles/standard/birds/ \
    -data_dir src/data/CUB_200_2011 \
    -dset birds \
    -e 1000 \
    -optimizer sgd \
    -pretrained \
    -use_aux \
    -b 64 \
    -weight_decay 0.0004 \
    -lr 0.01 \
    -scheduler_step 20
```

**输出路径：** `outputfiles/Standard/birds/312/False/1.0/`

**关键参数说明：**
| 参数 | 说明 |
|------|------|
| `-pretrained` | 加载 ImageNet 预训练权重（首次运行自动下载） |
| `-use_aux` | 使用 InceptionV3 辅助分类头（aux loss 权重 0.4） |
| `-e 1000` | 训练最多 1000 轮，内置早停（300轮无提升自动停止） |

---

## Step 4：实验二 —— Concept_XtoC（X→C 概念预测器）

训练只预测概念、不预测类别的瓶颈模型，用于 Sequential/Independent CBM 的第一阶段。

```bash
python3 experiments.py CUB Concept_XtoC --seed 1 \
    -log_dir outputfiles/concept_xtoc/birds/ \
    -data_dir src/data/CUB_200_2011 \
    -dset birds \
    -e 1000 \
    -optimizer sgd \
    -pretrained \
    -use_aux \
    -use_attr \
    -n_attributes 312 \
    -attr_loss_weight 1.0 \
    -normalize_loss \
    -b 64 \
    -weight_decay 0.0004 \
    -lr 0.01 \
    -scheduler_step 20 \
    -bottleneck
```

**关键参数说明：**
| 参数 | 说明 |
|------|------|
| `-use_attr` | 启用属性/概念预测头 |
| `-bottleneck` | 模型只输出概念预测（X→C），不接 C→Y 层 |
| `-n_attributes 312` | CUB 数据集使用全部 312 个概念 |
| `-normalize_loss` | 对多属性损失做归一化，避免梯度爆炸 |

---

## Step 5：实验三 —— Joint CBM（联合训练基线）

端到端联合训练概念预测器与分类器（X→C→Y），作为 CBM 标准基线。

```bash
python3 experiments.py CUB Joint --seed 1 \
    -log_dir outputfiles/joint/birds/ \
    -data_dir src/data/CUB_200_2011 \
    -dset birds \
    -e 1000 \
    -optimizer sgd \
    -pretrained \
    -use_aux \
    -use_attr \
    -n_attributes 312 \
    -attr_loss_weight 1.0 \
    -normalize_loss \
    -b 64 \
    -weight_decay 0.0004 \
    -lr 0.01 \
    -scheduler_step 20 \
    -use_sigmoid
```

**关键参数说明：**
| 参数 | 说明 |
|------|------|
| `-use_sigmoid` | C→Y 阶段对概念预测值过 Sigmoid 激活后再传入分类器 |

---

## Step 6：实验四 —— Coop-CBM（论文核心方法）

协作式多任务 CBM，同时输出主任务类别（Y）、辅助任务类别（Y'）和概念预测（C）。

### 6.1 Coop-CBM（不带 COL）—— 消融实验

```bash
python3 experiments.py CUB Coop --seed 1 \
    -log_dir outputfiles/coop/birds/ \
    -data_dir src/data/CUB_200_2011 \
    -dset birds \
    -e 1000 \
    -optimizer sgd \
    -pretrained \
    -use_aux \
    -use_attr \
    -n_attributes 312 \
    -attr_loss_weight 1.0 \
    -normalize_loss \
    -b 64 \
    -weight_decay 0.0004 \
    -lr 0.01 \
    -scheduler_step 20
```

### 6.2 Coop-CBM + COL（论文最终方法）

```bash
python3 experiments.py CUB Coop --seed 1 \
    -log_dir outputfiles/coop_col/birds/ \
    -data_dir src/data/CUB_200_2011 \
    -dset birds \
    -e 1000 \
    -optimizer sgd \
    -pretrained \
    -use_aux \
    -use_attr \
    -n_attributes 312 \
    -attr_loss_weight 1.0 \
    -normalize_loss \
    -b 64 \
    -weight_decay 0.0004 \
    -lr 0.01 \
    -scheduler_step 20 \
    -col \
    -col_w 1.0 \
    -gamma 0.5 \
    -attr_col
```

**COL 相关参数说明：**
| 参数 | 说明 |
|------|------|
| `-col` | 开启概念正交损失（COL） |
| `-col_w 1.0` | COL 损失整体权重，乘以 `loss_op` |
| `-gamma 0.5` | COL 中异类样本正交惩罚系数（`col.py`） |
| `-attr_col` | 对每个二元属性也施加 COL（同类属性为 1 的样本聚集） |

**损失函数组成：**
```
L_total = L_main(Y) + 0.4·L_aux(Y)         # 主任务 + 辅助任务（Inception aux）
        + L_coop(Y')                         # Coop 辅助分类头
        + attr_loss_weight · Σ L_concept(C)  # 312个概念的BCE损失
        + col_w · L_COL(features, Y)         # 类级别COL
        + 0.001 · Σ L_COL(features, C_i)    # 属性级别COL（attr_col开启时）
```

**训练中模型自动保存路径：**
```
outputfiles/{exp}/{dset}/{n_attributes}/{col_bool}/{attr_loss_weight}/best_model_{seed}.pth
# 例：outputfiles/Coop/birds/312/True/1.0/best_model_1.pth
```

---

## Step 7：实验五 —— TTI 测试时干预（可解释性核心评估）

TTI 通过逐步将模型预测错误的概念替换为真实标签，观察分类精度随干预次数的提升曲线，评估概念的语义可解释性。

**前置条件：** 需要 Step 6.2 训练完成的模型文件

```bash
python3 src/eval/tti.py \
    -log_dir outputfiles/tti_results/ \
    -dset birds \
    -model_dirs outputfiles/Coop/birds/312/True/1.0/best_model_1.pth \
    -eval_data test \
    -use_attr \
    -n_attributes 312 \
    -data_dir src/data/CUB_200_2011 \
    -data_dir2 src/data/CUB_200_2011 \
    -mode random \
    -n_trials 5 \
    -n_groups 28 \
    -use_sigmoid \
    -batch_size 16
```

### 参数说明

| 参数 | 可选值 | 说明 |
|------|--------|------|
| `-mode` | `random` / `entropy` | 随机干预 or 基于预测熵的贪心策略 |
| `-n_trials` | 整数（如 5） | `random` 模式下重复次数，取最大值 |
| `-n_groups` | 28 | CUB 的 28 个概念属性组 |
| `-class_level` | flag | 用类级别真实标签替换（vs 实例级别） |
| `-connect_CY` | flag | Coop 模型专用：C→Y 路径的连接方式 |

### 结果解读

输出保存到 `outputfiles/tti_results/results.txt`，格式：
```
0.0000  72.3400    ← 0组干预时的基础精度
1.0000  74.1200    ← 替换1组概念后的精度
2.0000  76.5800
...
28.0000 89.2100    ← 全部替换为真实概念时的精度上界
```

**曲线斜率越陡、恢复精度越快 → 概念语义可解释性越强**

### 对比多个模型

```bash
python3 src/eval/tti.py \
    -log_dir outputfiles/tti_compare/ \
    -model_dirs \
        outputfiles/Joint/birds/312/False/1.0/best_model_1.pth \
        outputfiles/Coop/birds/312/True/1.0/best_model_1.pth \
    -dset birds \
    ...（其余参数同上）
```

---

## Step 8：实验六 —— 分布偏移鲁棒性评估

### 8.1 图像损坏鲁棒性测试

在训练命令中添加 `-corruption_name` 参数，使用损坏后的图像评估泛化能力：

```bash
python3 experiments.py CUB Coop --seed 1 \
    -log_dir outputfiles/coop_corrupt/birds/ \
    -data_dir src/data/CUB_200_2011 \
    -dset birds \
    -e 1000 \
    -pretrained -use_aux -use_attr \
    -n_attributes 312 -attr_loss_weight 1.0 \
    -normalize_loss -b 64 \
    -weight_decay 0.0004 -lr 0.01 -scheduler_step 20 \
    -col -col_w 1.0 -gamma 0.5 \
    -corruption_name gaussian_blur    # 可选：gaussian_noise
    -optimizer sgd
```

可用的损坏类型（来自 `imagenet-c`）：
- `gaussian_blur`
- `gaussian_noise`
- 其他 imagenet-c 标准损坏类型

### 8.2 虚假相关性（Spurious Correlations）测试

使用 OOD 鸟类数据集（背景颜色与类别强相关），需要先准备 OOD 数据集：

```bash
# 使用 OOD 数据训练
python3 experiments.py CUB Coop --seed 1 \
    -log_dir outputfiles/coop_ood/birds/ \
    -data_dir /path/to/ood_bird_dataset \
    -dset birds-ood \
    -e 1000 \
    -pretrained -use_aux -use_attr \
    -n_attributes 312 -attr_loss_weight 1.0 \
    -normalize_loss -b 64 \
    -weight_decay 0.0004 -lr 0.01 \
    -scheduler_step 20 -optimizer sgd
```

### 8.3 噪声概念相关性测试

在 Coop 基础上通过 `-repeat_concepts` 引入概念噪声：

```bash
python3 experiments.py CUB Coop --seed 1 \
    -log_dir outputfiles/coop_noise/ \
    -data_dir src/data/CUB_200_2011 \
    -dset birds \
    -n_attributes 312 \
    -repeat_concepts \
    -rep 0.1 \          # 10% 的概念被重复（噪声相关）
    -e 1000 \
    -pretrained -use_aux -use_attr \
    -attr_loss_weight 1.0 -normalize_loss \
    -b 64 -weight_decay 0.0004 -lr 0.01 \
    -scheduler_step 20 -optimizer sgd
```

---

## Step 9：线性探针实验（Probe）

评估模型中间层特征是否包含可线性分离的概念信息，验证表示质量。

**前置条件：** 需要先从训练好的模型中提取 `representation_logits` 并保存为 pkl 文件

```bash
# 训练线性探针
python3 experiments.py CUB Probe --seed 1 \
    -data_dirs src/data/CUB_200_2011 \
    -log_dir outputfiles/probe_results/ \
    -n_attributes 312 \
    -b 64 \
    -e 1000 \
    -lr 0.01 \
    -weight_decay 5e-5 \
    -scheduler_step 1000

# 在测试集上评估探针
python3 experiments.py CUB Probe --seed 1 \
    -data_dirs src/data/CUB_200_2011 \
    -model_dirs outputfiles/probe_results/best_model.pth \
    -log_dir outputfiles/probe_eval/ \
    -n_attributes 312 \
    -b 64 \
    -eval
```

---

## Step 10：信息泄漏测试

通过对概念预测值做硬化（clipping to {0,1}）来验证模型是否通过概念层传递了额外的非语义信息。

- **Exp1（训练时裁剪）：** 在 Joint/Coop 训练中，修改 `template_model.py` 对 `attr_preds` 做 `torch.round()` 后再传入 C→Y
- **Exp2（评估时裁剪）：** 在 `inference.py` 的 `eval()` 中，对 `attr_outputs_sigmoid` 做阈值化后再传入 stage2

---

## 完整实验流程总览

```
实验流程（按顺序执行）
│
├── [环境准备]
│   ├── Step 0: 安装依赖
│   ├── Step 1: 补充 attributes 目录
│   └── Step 2: 修复代码 Bug
│
├── [基线对比训练]
│   ├── Step 3: Standard（黑盒上界）
│   ├── Step 4: Concept_XtoC（概念预测器）
│   └── Step 5: Joint CBM（标准基线）
│
├── [核心方法训练]
│   ├── Step 6.1: Coop（无COL，消融实验）
│   └── Step 6.2: Coop + COL（论文最终方法）
│
├── [评估与分析]
│   ├── Step 7: TTI 干预曲线（可解释性指标）
│   ├── Step 9: 线性探针（表示质量指标）
│   └── 信息泄漏测试
│
└── [泛化性评估]
    ├── Step 8.1: 图像损坏鲁棒性
    ├── Step 8.2: 虚假相关性 OOD
    └── Step 8.3: 噪声概念相关性
```

---

## 评估指标汇总

| 指标 | 计算位置 | 说明 |
|------|----------|------|
| Task Accuracy (Y) | `inference.py:eval()` | Top-1/3/5 类别分类精度 |
| Concept Accuracy (C) | `inference.py:eval()` | 312个概念的平均二分类精度 |
| Concept F1 | `inference.py:eval()` (feature_group_results) | 每个概念单独的 F1 分数分布 |
| TTI Curve | `tti.py:run()` | 精度 vs 干预组数曲线 |
| Linear Probe F1 | `probe.py:eval_linear_probe()` | 中间层特征可线性分离的概念信息量 |

---

## 常见问题速查

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| `AssertionError: Please specify valid experiment` | 'Coop' 不在允许列表 | 按 Step 2.1 修复 `experiments.py` |
| `FileNotFoundError: class_attribute_labels_continuous.txt` | attributes 目录缺失 | 按 Step 1.2 补充数据 |
| `ModuleNotFoundError: No module named 'analysis'` | 未在项目根目录运行 | `cd` 到根目录并设置 `PYTHONPATH` |
| `NameError: name 'probe' is not defined` | `train.py` 缺少导入 | 按 Step 2.2 修复 |
| TTI 运行时模型路径找不到 | 路径含 `col_bool`/`attr_loss_weight` 子目录 | 查看 `train.py:67` 的 `dir` 变量构建逻辑 |
| 训练中 loss 为 NaN | `attr_loss_weight` 过大 | 降低至 0.1 或开启 `-normalize_loss` |
